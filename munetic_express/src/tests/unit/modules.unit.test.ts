import errorHandler from '../../modules/errorHandler';

describe('module : errorHandler unit test', () => {
  it('err 타입이 function이면 새로운 ServiceResponse 객체를 생성해 응답한다.', () => {});
  it('err 타입이 ServiceResponse 이면 인스턴스 정보를 사용해 응답한다.', () => {});
});
