import styled from 'styled-components';

const Title = styled.p`
  margin: 1rem;
  padding-left: 1rem;
  font-size: 1.5rem;
  font-weight: 700;
  border-left: 4px solid rgb(82, 111, 255);
`;

export default Title;
